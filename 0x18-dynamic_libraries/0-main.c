#!/bin/bash
#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A sample program to test the _strlen function.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    printf("%d\n", _strlen("My Dyn Lib"));
    return (0);
}
