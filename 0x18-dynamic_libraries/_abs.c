#!/bin/bash
#include "main.h"

/*
 * _abs.c - Description of the file
 *
 * Description of each file individually.
 */
int _abs(int n)
{
    return (n >= 0 ? n : -n);
}

int _isupper(int c)
{
    return (c >= 'A' && c <= 'Z');
}
